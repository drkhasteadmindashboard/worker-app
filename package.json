{
  "name": "doctor-khaste-dashboard",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute doctor_khaste_db --remote --file=./schema.sql",
    "db:init:local": "wrangler d1 execute doctor_khaste_db --local --file=./schema.sql",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.120.0"
  }
}
